package com.ai.career.browser.interaction;

public enum InteractionActionType {
    FILL_TEXT,
    SELECT_OPTION,
    SELECT_MULTI_OPTION,
    CHECK,
    UNCHECK,
    UPLOAD_FILE,
    NO_ACTION,
    REQUIRES_REVIEW,
    UNSUPPORTED
}
